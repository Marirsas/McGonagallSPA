import { SortByAvailabilityPipe } from './sort-by-availability.pipe';

describe('SortByAvailabilityPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByAvailabilityPipe();
    expect(pipe).toBeTruthy();
  });
});
