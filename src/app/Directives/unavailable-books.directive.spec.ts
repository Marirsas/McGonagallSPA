import { UnavailableBooksDirective } from './unavailable-books.directive';

describe('UnavailableBooksDirective', () => {
  it('should create an instance', () => {
    const directive = new UnavailableBooksDirective();
    expect(directive).toBeTruthy();
  });
});
